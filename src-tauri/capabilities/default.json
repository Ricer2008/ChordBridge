{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "ChordBridge 主窗口默认权限：事件监听（关闭确认）等核心能力",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
