{
  "name": "chordbridge",
  "version": "0.1.0",
  "description": "吉他谱多维度转换与预览工具：简谱 / 五线谱 / 六线谱 / 和弦谱 互转 + MIDI 预览",
  "private": true,
  "scripts": {
    "web": "node scripts/serve.js",
    "tauri": "tauri",
    "build": "tauri build && bash scripts/package-macos.sh"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
